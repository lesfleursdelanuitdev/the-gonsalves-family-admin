/** Shared React Query key + API path for admin families hooks (avoids circular imports). */
export const ADMIN_FAMILIES_QUERY_KEY = ["admin", "families"] as const;
export const ADMIN_FAMILIES_API_BASE = "/api/admin/families";
