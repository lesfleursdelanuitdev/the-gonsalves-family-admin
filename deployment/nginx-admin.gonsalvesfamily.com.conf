# Reverse proxy for the-gonsalves-family-admin (Next.js on 127.0.0.1:3040)
#
# Install:
#   sudo cp deployment/nginx-admin.gonsalvesfamily.com.conf /etc/nginx/sites-available/admin.gonsalvesfamily.com
#   sudo ln -sf /etc/nginx/sites-available/admin.gonsalvesfamily.com /etc/nginx/sites-enabled/
#   sudo nginx -t && sudo systemctl reload nginx
#
# TLS: point DNS A/AAAA at this host, then e.g.
#   sudo certbot --nginx -d admin.gonsalvesfamily.com
# Certbot may edit this file; keep proxy_pass to 127.0.0.1:3040.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 80;
    listen [::]:80;
    server_name admin.gonsalvesfamily.com;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
        allow all;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name admin.gonsalvesfamily.com;

    ssl_certificate     /etc/letsencrypt/live/admin.gonsalvesfamily.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/admin.gonsalvesfamily.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    access_log /var/log/nginx/admin.gonsalvesfamily.com.access.log;
    error_log  /var/log/nginx/admin.gonsalvesfamily.com.error.log;

    # Large GEDCOM / media uploads via admin API (adjust if needed)
    client_max_body_size 128M;

    location / {
        proxy_pass http://127.0.0.1:3040;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
